# 证言取证 Demo 设计 Spec

## 1. 文档目的

本文档描述 `demo/forensic-web/` 当前演示原型的设计目标、信息架构、核心交互、状态规则、审计事件和导出结构，用于：

- 统一演示产品的交互定义
- 作为前端原型、后端联调和讲解演示的共同依据
- 说明“案件情况分析文件 -> 语音转写 -> 风险提示依据链 -> 人工复核 -> 导出证据包”的完整闭环

配套帮助文档见：

- [demo/forensic-web/README.md](demo/forensic-web/README.md)

## 2. 产品目标

这不是普通字幕工具 Demo，而是“语音证据链演示原型”。

演示必须稳定传达 5 个核心保证：

1. 原始语音证据不可删除、不可覆盖，只能归档
2. 分析材料不可删除，上传后保留水印记录，只能选择是否作为参考
3. 风险提示不是黑盒，而是能解释“根据哪份材料、哪条规则、为什么提示”
4. 人工修订不会覆盖机器初稿，而是形成新的可审计版本
5. 导出的是证据包，而不是单一文本

## 3. 演示边界

本 Demo 是前端静态原型，主要展示交互与证据链表达，不负责：

- 真正的 PDF / Word 内容解析
- 真正的 Whisper / WhisperX 推理
- 真正的电子签章、WORM 存储、RBAC 权限
- 真正的法律文书生成

当前风险提示依据链基于：

- 证据片段标签
- 证据文本关键词
- 已选案件情况分析文件的主题映射

## 4. 信息架构

当前全局导航只有 2 个入口：

1. `案件中心`
2. `当前案件工作台`

不再把“案件详情 / 转写复核 / 审计与导出”做成全局并列页面。

### 4.1 案件中心

定位：

- 从案件维度管理整个证据流
- 作为所有演示路径的起点

主要内容：

- 案件搜索 / 状态筛选
- 案件表格
- KPI 和状态分布

核心动作：

- `进入案件`
- `新建演示案件`

### 4.2 当前案件工作台

定位：

- 在一个工作台中串联案件背景、分析材料、语音证据、阶段处理和审计信息

主要内容顺序：

1. 案件基础信息
2. 案件情况分析文件
3. 新增语音证据
4. 语音证据列表
5. 当前证据阶段入口
6. 当前阶段内容区
7. 案件最近活动

## 5. 核心对象模型

### 5.1 Case

字段概念：

- `id`
- `name`
- `officer`
- `referenceMaterials[]`
- `evidences[]`
- `audit[]`

### 5.2 ReferenceMaterial

定位：

- 案件情况分析文件
- 用于后续语音证据风险提示的本地参考材料

关键字段：

- `id`
- `name`
- `fileType`
- `uploadedAt`
- `uploadedBy`
- `watermarkId`
- `selectedAsReference`
- `themes[]`
- `insight`

约束：

- 不允许删除
- 允许切换是否纳入参考
- 上传后必须写入水印记录和审计事件

### 5.3 Evidence

定位：

- 单条语音证据

关键字段：

- `id`
- `name`
- `uploadedAt`
- `uploadActor`
- `sha256`
- `duration`
- `stage`
- `archived`
- `transcriptionRuns`
- `segments[]`
- `edits[]`
- `confirmedAt`
- `exportedAt`

约束：

- 不允许删除
- 允许归档
- `查看` 负责切换当前证据上下文

### 5.4 Segment

关键字段：

- `id`
- `speaker`
- `start`
- `end`
- `text`
- `tags[]`

### 5.5 Risk Reference Context

定位：

- 当前证据的风险提示依据链

关键字段：

- `selectedMaterials[]`
- `findings[]`
- `hitSegments`
- `totalHits`
- `materialStats[]`

其中 `findings[]` 结构包含：

- `segmentId`
- `themes[]`
- `hits[]`

每条 `hit` 包含：

- `materialId`
- `materialName`
- `watermarkId`
- `theme`
- `rule`
- `reason`

## 6. 当前案件工作台交互规则

### 6.1 案件基础信息

展示：

- 案件编号
- 调查员
- 案件状态
- 证据策略

固定文案原则：

- 必须明显显示“原始证据不可删除，仅可归档”

### 6.2 案件情况分析文件

目的：

- 补全语音证据分析前的“背景材料”层

支持格式：

- PDF
- DOC
- DOCX
- TXT
- MD
- RTF
- ODT

交互规则：

1. 上传后新增一条材料记录
2. 自动生成 `watermarkId`
3. 默认 `selectedAsReference = false`
4. 只能切换“选为参考 / 取消参考”
5. 不提供删除

显示字段：

- 材料 ID
- 文件名
- 类型
- 上传时间
- 水印记录
- 参考状态
- 风险分析主题
- 操作

按钮定义：

- `选为参考`
- `取消参考`

### 6.3 新增语音证据

交互规则：

1. 上传后新增一条语音证据
2. 计算 SHA-256
3. 自动推进到 `待人工复核`
4. 如果当前案件存在已选参考材料，则写入 `analysis_reference_attached`

### 6.4 语音证据列表

交互规则：

- 默认隐藏归档记录
- 通过勾选 `显示已归档` 展示归档证据
- 列表操作只保留：
  - `查看`
  - `归档`

说明：

- `查看` 的作用是切换“当前证据”
- 当前证据切换后，阶段入口和内容区同步切换到该证据的当前阶段
- 不再保留 `进入流程`

### 6.5 阶段入口

阶段固定为：

1. `原始证据已固化`
2. `自动转写完成`
3. `待人工复核`
4. `已形成确认稿`

约束规则：

- 当前证据只能进入自身阶段及之前阶段
- 比当前阶段更靠后的阶段按钮必须禁用

## 7. 分阶段交互 Spec

### 7.1 Step 1：原始证据已固化

目的：

- 证明原始证据已经保全

展示：

- 证据元数据
- SHA-256 全量值
- 上传时间 / 上传人
- 只读说明

允许操作：

- 查看哈希详情
- 归档

禁止操作：

- 删除
- 编辑

### 7.2 Step 2：自动转写完成

目的：

- 展示机器转写结果和“风险提示依据”的来源

展示：

- 转写轮次
- 分段数量
- 命中参考依据片段数
- 风险提示依据卡
- 每个分段的命中依据列表
- 转写任务日志

说明：

- 这里的风险提示依据必须可解释
- 不能只显示“命中主题”，必须能展开到材料 / 规则 / 原因

允许操作：

- 查看
- 重跑转写（演示）
- 归档

### 7.3 Step 3：待人工复核

目的：

- 在人工复核时清楚说明“系统为什么提示这里有风险”

展示：

- 音频播放器
- 时间轴
- 机器初稿列表
- 当前片段的风险提示依据链
- 原始文本 / 修订文本 / 修改原因 / diff

当前片段的依据链必须展示：

- 命中的材料 ID 和材料名
- 命中的规则
- 触发提示的原因说明

允许操作：

- 片段切换
- 风险跳转
- 文本修订
- 选择修改原因
- 提交复核
- 归档

提交复核后：

- 写入 `transcript_edited`
- 写入 `review_confirmed`
- 当前证据进入 Step 4

### 7.4 Step 4：已形成确认稿

目的：

- 对外展示当前证据已经形成可导出的确认稿

展示：

- 版本号
- 确认时间
- 导出状态
- 风险提示依据摘要
- 审计摘要

允许操作：

- 导出结构化 JSON
- 标记已导出
- 查看审计链
- 归档

导出 JSON 必须包含：

- `reference_materials`
- `selected_reference_materials`
- `risk_reference_context`
- `evidence`
- `segments`
- `edits`
- `audit`

## 8. 风险提示依据链规则

### 8.1 输入来源

风险提示依据链由以下两类输入组成：

1. 片段本身的标签和文本内容
2. 已选案件情况分析文件的分析主题

### 8.2 主题到规则的映射

当前原型内置以下主题和规则：

- `日期核验` -> `时间节点核验`
- `人物接触` -> `接触关系识别`
- `金额核验` -> `金额数值比对`
- `资金流向` -> `资金路径匹配`
- `证言冲突` -> `证言一致性检查`
- `人物关系` -> `人物关联比对`

### 8.3 命中说明

每次命中必须生成一条 `hit`，包含：

- 材料来源
- 命中主题
- 规则名
- 原因说明

演示要求：

- 任何风险提示都必须能追溯到至少一条 `hit`
- 如果没有选参考材料，必须明确提示退回“基础规则”

## 9. 审计事件 Spec

### 9.1 案件与材料

- `case_created`
- `analysis_file_uploaded`
- `analysis_watermarked`
- `reference_material_selected`
- `reference_material_unselected`

### 9.2 语音证据

- `audio_uploaded`
- `hash_generated`
- `transcription_completed`
- `transcription_rerun`
- `analysis_reference_attached`
- `evidence_archived`

### 9.3 人工复核与导出

- `transcript_edited`
- `review_confirmed`
- `evidence_exported`

## 10. 导出结构 Spec

导出 JSON 至少应包含以下顶层字段：

```json
{
  "case_id": "CASE-2026-001",
  "case_name": "廉政调查模拟案A",
  "officer": "officer_001",
  "reference_materials": [],
  "selected_reference_materials": [],
  "risk_reference_context": {},
  "evidence": {},
  "segments": [],
  "edits": [],
  "audit": []
}
```

其中 `risk_reference_context.findings[].hits[]` 必须保留：

- `materialId`
- `materialName`
- `watermarkId`
- `theme`
- `rule`
- `reason`

## 11. 内置演示数据要求

为了保证演示效果，默认数据至少覆盖：

1. 一个含多条证据和多份参考材料的案件
2. 一个“自动转写完成但未选参考材料”的案件
3. 一个“仅原始证据已固化”的案件
4. 一个可通过“新建演示案件”现场上传并完成复核的空白案件

## 12. 推荐演示顺序

1. `CASE-2026-001`
   - 看分析材料
   - 看不同证据
   - 看 Step 3 的依据链
   - 看 Step 4 的摘要和导出
2. `CASE-2026-003`
   - 演示阶段锁定
   - 演示“缺少分析依据”提示
3. `CASE-2026-004`
   - 演示原始证据保全
4. 新建演示案件
   - 上传分析文件
   - 选为参考
   - 上传音频
   - 进入复核
   - 导出确认稿

## 13. 后续可继续增强的方向

当前最值得继续增强的点：

1. 让原因说明带上“案情摘要第 X 类要点”之类更正式的法务表达
2. 为参考材料增加预览入口和材料摘要
3. 把风险提示依据摘要扩展为证据包内单独章节
4. 为每条编辑补充“回放时间点 + 修改前后版本号”

## 14. 非功能要求

作为演示产品，界面应满足：

- 结构清楚，优先表达证据链而不是模型细节
- 原始证据只读策略必须在页面上持续可见
- 多条证据和多份参考材料同时存在时仍然不混乱
- 移动端不要求完整审阅体验，但页面布局不能崩坏
