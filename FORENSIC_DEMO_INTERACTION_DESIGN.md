# 证言取证演示产品交互设计（演示优先版）

## 1. 目标与演示原则

本设计面向“高保密 + 高准确 + 可追溯”的证言取证场景，目标不是展示 ASR 模型参数，而是完整演示证据链闭环：

1. 原始证据只读（不可覆盖）
2. 音频与文本可双向定位（可回放验证）
3. 所有人工修改可审计（版本化 + 时间线）
4. 导出的是“证据包”而不是单一文本

### 演示时要反复强调的三句话

- 原始音频永不修改。
- 所有文本都可回放定位。
- 所有修改都有审计记录。

---

## 2. 信息架构（4 页最小闭环）

建议仅保留以下四个一级页面，避免演示分散：

- 案件列表
- 案件详情
- 转写复核
- 审计与导出

### 全局常驻状态栏（顶部）

- 案件编号：`CASE-2026-001`
- 当前状态：`待人工复核`
- 证据策略：`原始证据只读`
- 导出状态：`未导出`

---

## 3. 页面一：案件列表

### 页面目标
将产品定位为“案件系统”，而不是“上传工具”。

### 关键区块

1. 搜索 + 筛选
   - 搜索：案件编号 / 证人 / 调查员
   - 筛选：状态、风险等级
2. 案件表格
   - 案件编号
   - 案件名称
   - 证据数
   - 当前状态
   - 最近更新时间
3. 今日概览卡
   - 待复核数
   - 低置信片段数
   - 待确认案件数
   - 今日导出数

### 核心交互

- `进入案件` → 跳转案件详情
- `新建演示案件` → 快速创建 Demo Case

### 关键文案（可直接用于 UI）

- 页面标题：`案件中心`
- 次标题：`所有证据围绕案件流转，支持全流程追溯`
- 空状态：`暂无案件，创建一个演示案件开始证据链流程`

---

## 4. 页面二：案件详情

### 页面目标
突出“证据固化成功”这一关键价值点。

### 布局建议

- 左栏：案件信息 + 流程进度 + 最近活动
- 右栏：证据列表 + 上传入口 + 固化摘要

### 必须展示的数据

- 证据文件名 / 时长 / 上传人 / 上传时间
- `SHA-256` 摘要（默认折叠，支持展开全文）
- 存证状态：`已固化`
- 处理状态：`已上传 → 已哈希 → 已转写 → 待复核`

### 核心交互

- 上传后立即显示“证据固化成功卡片”
- `查看证据链` → 跳转审计与导出页
- `开始复核` → 跳转转写复核页

### 关键文案

- 顶部徽标：`原始证据只读`
- 卡片标题：`证据固化成功`
- 提示：`后续处理基于副本进行，原始证据不可修改`

---

## 5. 页面三：转写复核（核心页）

### 页面目标
完成“音频 ↔ 文本 ↔ 修订 ↔ 审计”一体化演示。

### 三栏布局

1. 左栏（音频）
   - 播放器
   - 波形图
   - 当前时间
   - 播放速度
   - 上一处/下一处风险片段
2. 中栏（机器初稿）
   - Segment 列表（speaker + start/end + text）
   - 标签：低置信 / 金额 / 日期 / 人名 / 需复核
   - 当前播放句高亮
3. 右栏（修订面板）
   - 原始文本（只读）
   - 修订文本（可编辑）
   - 修改原因（必选）
   - diff 预览
   - 保存修订

### 核心交互规则

- 点击任一句子 → 跳转音频到对应时间
- 拖动播放进度 → 自动滚动定位当前句
- 保存修订必须选择原因：
  - 回放确认
  - 口误修正
  - 补全日期
  - 更正金额
  - 其他
- 保存后立刻给出反馈：`已生成审计记录`

### 关键文案

- 中栏标题：`机器初稿`（不要写“最终稿”）
- 右栏标题：`人工修订（生成新版本）`
- 保存按钮：`保存修订并写入审计`

---

## 6. 页面四：审计与导出

### 页面目标
以“证据履历”完成可信收口，支持对外展示。

### 上半区：审计时间线

每条记录展示：

- 时间
- 操作人
- 动作
- 影响对象
- 摘要（支持跳转原片段）

建议事件类型：

- case_created
- audio_uploaded
- hash_generated
- transcription_started
- transcription_completed
- transcript_edited
- transcript_reviewed
- evidence_exported

### 下半区：导出面板

导出前显示内容清单：

- 原始音频
- 机器初稿 JSON
- 人工确认稿 JSON
- 审计日志 JSON
- 哈希清单
- 正式证言 PDF

支持三类导出按钮：

- 导出证据包 ZIP
- 导出正式证言 PDF
- 导出结构化 JSON

### 关键文案

- 状态提示：`证据包生成成功，校验摘要已写入`
- 操作说明：`点击时间线事件可跳转到对应证据或文本片段`

---

## 7. 页面状态机（建议用于前后端联调）

主状态建议统一为：

1. `原始证据已固化`
2. `自动转写完成`
3. `待人工复核`
4. `已形成确认稿`
5. `证据包已导出`

### 迁移规则（简化版）

- 上传成功 + 哈希落库 → `原始证据已固化`
- 转写/对齐/说话人完成 → `自动转写完成`
- 任一高风险片段未处理 → `待人工复核`
- 复核通过并确认 → `已形成确认稿`
- 导出成功并写入导出日志 → `证据包已导出`

---

## 8. 演示脚本（5-8 分钟）

1. 打开案件列表：强调案件视角管理。  
2. 进入案件详情并上传音频：强调“即时哈希 + 固化 + 原始只读”。  
3. 进入转写复核：点击一句低置信文本回放，修改日期或金额，保存并展示审计提示。  
4. 打开审计与导出：展示时间线、导出清单、导出成功状态。  
5. 总结：最终文本可追溯到原始音频和全部人工行为。

---

## 9. 高保真文案包（可直接交设计/前端）

### 通用术语（请统一）

- 原始证据
- 机器初稿
- 人工确认稿
- 审计记录
- 证据包

### 按钮文案建议

- 新建演示案件
- 上传原始音频
- 开始复核
- 保存修订并写入审计
- 查看证据链
- 导出证据包 ZIP
- 导出正式证言 PDF

### Toast / 状态提示

- `证据已固化：SHA-256 已记录`
- `机器初稿生成完成，请进行人工复核`
- `修订已保存并写入审计记录`
- `确认稿已生成，可导出证据包`
- `导出成功，校验摘要已附加`

---

## 10. 组件清单（MVP）

- CaseTable（案件表）
- EvidenceCard（证据卡）
- EvidenceHashPanel（哈希摘要）
- ProcessingStepper（证据流程条）
- AudioWavePlayer（音频+波形）
- TranscriptSegmentList（分段文本）
- RiskTag（风险标签）
- EditDiffPanel（修订差异）
- AuditTimeline（审计时间线）
- ExportChecklist（导出内容清单）

> 建议优先实现：`ProcessingStepper`、`EditDiffPanel`、`AuditTimeline`。这三个组件最能体现“证据可追溯”差异化。
